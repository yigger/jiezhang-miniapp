$header-height ?= 68PX
$bottom-height ?= 60PX

.page-root-component {
  position relative
  display flex
  flex-direction column
  // height 100vh

  > .page-root__header-component {
    display flex
    align-items center
    width 100%
    height $header-height

    padding-left 1em
    padding-right 100PX
    background $primaryBgColor
    color $primaryBgTextColor
    transition $header-height 200ms

    .header-title {
      color: $primaryBgTextColor
    }
  }

  > .page-root__tab-bar-component {
    position fixed
    left 0
    bottom 0
    right 0
    z-index 999
    background #ffffff

    display flex
    align-items center
    height $bottom-height
    width 100%
    border-top: 1PX solid $borderColor
    > view {
      &:not(:last-child) {
        height: 100%
        border-right: 1PX solid #f9f9f9
      }

      &.active {
        color: $primaryColor
      }
    }
  }

  > .page-root__main-content {
    flex: 1
    margin-bottom $bottom-height
    // max-height: "calc(100% - %s - %s)" % ($header-height $bottom-height)
    // overflow: scroll
  }
}